<Window x:Class="TrayWebApps.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orbit Password" Width="360" Height="220" MinWidth="360" MinHeight="220"
        WindowStyle="None" ResizeMode="NoResize" Background="#0B0F15"
        WindowStartupLocation="CenterOwner" Topmost="True">
  <Border BorderBrush="#323C4E" BorderThickness="1" Background="#0B0F15">
    <Grid Margin="22">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" Text="Password required" Foreground="{DynamicResource Ink}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,6" />
      <TextBlock Grid.Row="1" Text="Enter the password to close Orbit." Foreground="{DynamicResource MutedInk}" FontSize="12" Margin="0,0,0,14" />
      <PasswordBox x:Name="PasswordInput" Grid.Row="2" KeyDown="PasswordInput_KeyDown" />
      <TextBlock x:Name="ErrorText" Grid.Row="3" Text="Incorrect password." Foreground="#FF8D98" FontSize="12" Margin="2,8,2,0" Visibility="Collapsed" />
      <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
        <Button Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0" />
        <Button Content="Unlock" Click="Unlock_Click" Background="#24483D" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
