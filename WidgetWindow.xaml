<Window x:Class="TrayWebApps.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orbit Widget" WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False" Icon="Assets/AppIcon.ico"
        MouseLeftButtonDown="Widget_MouseLeftButtonDown">
  <Border x:Name="WidgetBorder" CornerRadius="10" Background="#26372F" BorderBrush="#3A4A40" BorderThickness="1" ClipToBounds="True">
    <Grid Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition x:Name="ImageRow" Height="*" />
        <RowDefinition x:Name="TextRow" Height="Auto" />
      </Grid.RowDefinitions>
      <Image x:Name="WidgetImage" Grid.Row="0" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
      <TextBlock x:Name="WidgetLabel" Grid.Row="1" Foreground="{DynamicResource Ink}" FontWeight="SemiBold" FontSize="14"
                 HorizontalAlignment="Center" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextAlignment="Center" TextWrapping="Wrap" />
    </Grid>
  </Border>
</Window>
